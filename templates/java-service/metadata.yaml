spec:
  name: {{SERVICE_NAME}}
  short_name: {{SHORT_NAME}}  # Short name for CLI convenience (e.g., 'myapp' for 'myapp-service')
  description: {{SERVICE_DESCRIPTION}}
  type: java
  port: {{PORT}}
  cd: true
  codeowners:
    - "@{{TEAM_NAME}}"
test:
  coverage: 30
