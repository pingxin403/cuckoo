apiVersion: v1
kind: Service
metadata:
  name: flash-sale-service
  namespace: default
  labels:
    app: flash-sale-service
spec:
  type: ClusterIP
  ports:
  - port: 9094
    targetPort: 9094
    protocol: TCP
    name: grpc
  selector:
    app: flash-sale-service
