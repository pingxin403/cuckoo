// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.4
// source: auth.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "auth.v1";

/** AuthErrorCode defines error codes for authentication failures. */
export enum AuthErrorCode {
  /** AUTH_ERROR_CODE_UNSPECIFIED - No error */
  AUTH_ERROR_CODE_UNSPECIFIED = 0,
  /** AUTH_ERROR_CODE_TOKEN_EXPIRED - Token is expired */
  AUTH_ERROR_CODE_TOKEN_EXPIRED = 1,
  /** AUTH_ERROR_CODE_INVALID_SIGNATURE - Token signature is invalid */
  AUTH_ERROR_CODE_INVALID_SIGNATURE = 2,
  /** AUTH_ERROR_CODE_MALFORMED_TOKEN - Token format is malformed */
  AUTH_ERROR_CODE_MALFORMED_TOKEN = 3,
  /** AUTH_ERROR_CODE_MISSING_CLAIMS - Token claims are missing required fields */
  AUTH_ERROR_CODE_MISSING_CLAIMS = 4,
  /** AUTH_ERROR_CODE_INVALID_REFRESH_TOKEN - Refresh token is invalid or expired */
  AUTH_ERROR_CODE_INVALID_REFRESH_TOKEN = 5,
  /** AUTH_ERROR_CODE_INTERNAL_ERROR - Internal server error */
  AUTH_ERROR_CODE_INTERNAL_ERROR = 6,
  /** AUTH_ERROR_CODE_INVALID_TOKEN - Generic invalid token error */
  AUTH_ERROR_CODE_INVALID_TOKEN = 7,
  UNRECOGNIZED = -1,
}

export function authErrorCodeFromJSON(object: any): AuthErrorCode {
  switch (object) {
    case 0:
    case "AUTH_ERROR_CODE_UNSPECIFIED":
      return AuthErrorCode.AUTH_ERROR_CODE_UNSPECIFIED;
    case 1:
    case "AUTH_ERROR_CODE_TOKEN_EXPIRED":
      return AuthErrorCode.AUTH_ERROR_CODE_TOKEN_EXPIRED;
    case 2:
    case "AUTH_ERROR_CODE_INVALID_SIGNATURE":
      return AuthErrorCode.AUTH_ERROR_CODE_INVALID_SIGNATURE;
    case 3:
    case "AUTH_ERROR_CODE_MALFORMED_TOKEN":
      return AuthErrorCode.AUTH_ERROR_CODE_MALFORMED_TOKEN;
    case 4:
    case "AUTH_ERROR_CODE_MISSING_CLAIMS":
      return AuthErrorCode.AUTH_ERROR_CODE_MISSING_CLAIMS;
    case 5:
    case "AUTH_ERROR_CODE_INVALID_REFRESH_TOKEN":
      return AuthErrorCode.AUTH_ERROR_CODE_INVALID_REFRESH_TOKEN;
    case 6:
    case "AUTH_ERROR_CODE_INTERNAL_ERROR":
      return AuthErrorCode.AUTH_ERROR_CODE_INTERNAL_ERROR;
    case 7:
    case "AUTH_ERROR_CODE_INVALID_TOKEN":
      return AuthErrorCode.AUTH_ERROR_CODE_INVALID_TOKEN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AuthErrorCode.UNRECOGNIZED;
  }
}

export function authErrorCodeToJSON(object: AuthErrorCode): string {
  switch (object) {
    case AuthErrorCode.AUTH_ERROR_CODE_UNSPECIFIED:
      return "AUTH_ERROR_CODE_UNSPECIFIED";
    case AuthErrorCode.AUTH_ERROR_CODE_TOKEN_EXPIRED:
      return "AUTH_ERROR_CODE_TOKEN_EXPIRED";
    case AuthErrorCode.AUTH_ERROR_CODE_INVALID_SIGNATURE:
      return "AUTH_ERROR_CODE_INVALID_SIGNATURE";
    case AuthErrorCode.AUTH_ERROR_CODE_MALFORMED_TOKEN:
      return "AUTH_ERROR_CODE_MALFORMED_TOKEN";
    case AuthErrorCode.AUTH_ERROR_CODE_MISSING_CLAIMS:
      return "AUTH_ERROR_CODE_MISSING_CLAIMS";
    case AuthErrorCode.AUTH_ERROR_CODE_INVALID_REFRESH_TOKEN:
      return "AUTH_ERROR_CODE_INVALID_REFRESH_TOKEN";
    case AuthErrorCode.AUTH_ERROR_CODE_INTERNAL_ERROR:
      return "AUTH_ERROR_CODE_INTERNAL_ERROR";
    case AuthErrorCode.AUTH_ERROR_CODE_INVALID_TOKEN:
      return "AUTH_ERROR_CODE_INVALID_TOKEN";
    case AuthErrorCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** ValidateTokenRequest contains the JWT token to be validated. */
export interface ValidateTokenRequest {
  /** JWT access token to validate */
  accessToken: string;
}

/** ValidateTokenResponse contains the validation result and extracted claims. */
export interface ValidateTokenResponse {
  /** Whether the token is valid */
  valid: boolean;
  /** User ID extracted from token claims (empty if invalid) */
  userId: string;
  /**
   * Device ID extracted from token claims (empty if invalid)
   * Device ID is a UUID v4 format identifier for the user's device
   */
  deviceId: string;
  /** Token expiration time */
  expiresAt:
    | Date
    | undefined;
  /** Error code if validation failed */
  errorCode: AuthErrorCode;
  /** Human-readable error message */
  errorMessage: string;
}

/** RefreshTokenRequest contains the refresh token to generate new access token. */
export interface RefreshTokenRequest {
  /** Refresh token */
  refreshToken: string;
}

/** RefreshTokenResponse contains the new token pair. */
export interface RefreshTokenResponse {
  /** New access token */
  accessToken: string;
  /** New refresh token */
  refreshToken: string;
  /** Access token expiration time */
  accessTokenExpiresAt:
    | Date
    | undefined;
  /** Refresh token expiration time */
  refreshTokenExpiresAt:
    | Date
    | undefined;
  /** Error code if refresh failed */
  errorCode: AuthErrorCode;
  /** Human-readable error message */
  errorMessage: string;
}

/** JWT claims structure containing user and device information. */
export interface JWTClaims {
  /** User ID */
  userId: string;
  /** Device ID (UUID v4 format) */
  deviceId: string;
  /** Token issued at timestamp */
  issuedAt:
    | Date
    | undefined;
  /** Token expiration timestamp */
  expiresAt:
    | Date
    | undefined;
  /** Token issuer */
  issuer: string;
  /** Token subject (typically user_id) */
  subject: string;
}

function createBaseValidateTokenRequest(): ValidateTokenRequest {
  return { accessToken: "" };
}

export const ValidateTokenRequest: MessageFns<ValidateTokenRequest> = {
  encode(message: ValidateTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accessToken !== "") {
      writer.uint32(10).string(message.accessToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidateTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidateTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValidateTokenRequest {
    return {
      accessToken: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
        ? globalThis.String(object.access_token)
        : "",
    };
  },

  toJSON(message: ValidateTokenRequest): unknown {
    const obj: any = {};
    if (message.accessToken !== "") {
      obj.accessToken = message.accessToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidateTokenRequest>, I>>(base?: I): ValidateTokenRequest {
    return ValidateTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidateTokenRequest>, I>>(object: I): ValidateTokenRequest {
    const message = createBaseValidateTokenRequest();
    message.accessToken = object.accessToken ?? "";
    return message;
  },
};

function createBaseValidateTokenResponse(): ValidateTokenResponse {
  return { valid: false, userId: "", deviceId: "", expiresAt: undefined, errorCode: 0, errorMessage: "" };
}

export const ValidateTokenResponse: MessageFns<ValidateTokenResponse> = {
  encode(message: ValidateTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.valid !== false) {
      writer.uint32(8).bool(message.valid);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.deviceId !== "") {
      writer.uint32(26).string(message.deviceId);
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(34).fork()).join();
    }
    if (message.errorCode !== 0) {
      writer.uint32(40).int32(message.errorCode);
    }
    if (message.errorMessage !== "") {
      writer.uint32(50).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidateTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidateTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.valid = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.errorCode = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValidateTokenResponse {
    return {
      valid: isSet(object.valid) ? globalThis.Boolean(object.valid) : false,
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      deviceId: isSet(object.deviceId)
        ? globalThis.String(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.String(object.device_id)
        : "",
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
      errorCode: isSet(object.errorCode)
        ? authErrorCodeFromJSON(object.errorCode)
        : isSet(object.error_code)
        ? authErrorCodeFromJSON(object.error_code)
        : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
    };
  },

  toJSON(message: ValidateTokenResponse): unknown {
    const obj: any = {};
    if (message.valid !== false) {
      obj.valid = message.valid;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    if (message.errorCode !== 0) {
      obj.errorCode = authErrorCodeToJSON(message.errorCode);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidateTokenResponse>, I>>(base?: I): ValidateTokenResponse {
    return ValidateTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidateTokenResponse>, I>>(object: I): ValidateTokenResponse {
    const message = createBaseValidateTokenResponse();
    message.valid = object.valid ?? false;
    message.userId = object.userId ?? "";
    message.deviceId = object.deviceId ?? "";
    message.expiresAt = object.expiresAt ?? undefined;
    message.errorCode = object.errorCode ?? 0;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseRefreshTokenRequest(): RefreshTokenRequest {
  return { refreshToken: "" };
}

export const RefreshTokenRequest: MessageFns<RefreshTokenRequest> = {
  encode(message: RefreshTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.refreshToken !== "") {
      writer.uint32(10).string(message.refreshToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshTokenRequest {
    return {
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
        ? globalThis.String(object.refresh_token)
        : "",
    };
  },

  toJSON(message: RefreshTokenRequest): unknown {
    const obj: any = {};
    if (message.refreshToken !== "") {
      obj.refreshToken = message.refreshToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RefreshTokenRequest>, I>>(base?: I): RefreshTokenRequest {
    return RefreshTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RefreshTokenRequest>, I>>(object: I): RefreshTokenRequest {
    const message = createBaseRefreshTokenRequest();
    message.refreshToken = object.refreshToken ?? "";
    return message;
  },
};

function createBaseRefreshTokenResponse(): RefreshTokenResponse {
  return {
    accessToken: "",
    refreshToken: "",
    accessTokenExpiresAt: undefined,
    refreshTokenExpiresAt: undefined,
    errorCode: 0,
    errorMessage: "",
  };
}

export const RefreshTokenResponse: MessageFns<RefreshTokenResponse> = {
  encode(message: RefreshTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accessToken !== "") {
      writer.uint32(10).string(message.accessToken);
    }
    if (message.refreshToken !== "") {
      writer.uint32(18).string(message.refreshToken);
    }
    if (message.accessTokenExpiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.accessTokenExpiresAt), writer.uint32(26).fork()).join();
    }
    if (message.refreshTokenExpiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.refreshTokenExpiresAt), writer.uint32(34).fork()).join();
    }
    if (message.errorCode !== 0) {
      writer.uint32(40).int32(message.errorCode);
    }
    if (message.errorMessage !== "") {
      writer.uint32(50).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.accessTokenExpiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.refreshTokenExpiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.errorCode = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshTokenResponse {
    return {
      accessToken: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
        ? globalThis.String(object.access_token)
        : "",
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
        ? globalThis.String(object.refresh_token)
        : "",
      accessTokenExpiresAt: isSet(object.accessTokenExpiresAt)
        ? fromJsonTimestamp(object.accessTokenExpiresAt)
        : isSet(object.access_token_expires_at)
        ? fromJsonTimestamp(object.access_token_expires_at)
        : undefined,
      refreshTokenExpiresAt: isSet(object.refreshTokenExpiresAt)
        ? fromJsonTimestamp(object.refreshTokenExpiresAt)
        : isSet(object.refresh_token_expires_at)
        ? fromJsonTimestamp(object.refresh_token_expires_at)
        : undefined,
      errorCode: isSet(object.errorCode)
        ? authErrorCodeFromJSON(object.errorCode)
        : isSet(object.error_code)
        ? authErrorCodeFromJSON(object.error_code)
        : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
    };
  },

  toJSON(message: RefreshTokenResponse): unknown {
    const obj: any = {};
    if (message.accessToken !== "") {
      obj.accessToken = message.accessToken;
    }
    if (message.refreshToken !== "") {
      obj.refreshToken = message.refreshToken;
    }
    if (message.accessTokenExpiresAt !== undefined) {
      obj.accessTokenExpiresAt = message.accessTokenExpiresAt.toISOString();
    }
    if (message.refreshTokenExpiresAt !== undefined) {
      obj.refreshTokenExpiresAt = message.refreshTokenExpiresAt.toISOString();
    }
    if (message.errorCode !== 0) {
      obj.errorCode = authErrorCodeToJSON(message.errorCode);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RefreshTokenResponse>, I>>(base?: I): RefreshTokenResponse {
    return RefreshTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RefreshTokenResponse>, I>>(object: I): RefreshTokenResponse {
    const message = createBaseRefreshTokenResponse();
    message.accessToken = object.accessToken ?? "";
    message.refreshToken = object.refreshToken ?? "";
    message.accessTokenExpiresAt = object.accessTokenExpiresAt ?? undefined;
    message.refreshTokenExpiresAt = object.refreshTokenExpiresAt ?? undefined;
    message.errorCode = object.errorCode ?? 0;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseJWTClaims(): JWTClaims {
  return { userId: "", deviceId: "", issuedAt: undefined, expiresAt: undefined, issuer: "", subject: "" };
}

export const JWTClaims: MessageFns<JWTClaims> = {
  encode(message: JWTClaims, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.deviceId !== "") {
      writer.uint32(18).string(message.deviceId);
    }
    if (message.issuedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.issuedAt), writer.uint32(26).fork()).join();
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(34).fork()).join();
    }
    if (message.issuer !== "") {
      writer.uint32(42).string(message.issuer);
    }
    if (message.subject !== "") {
      writer.uint32(50).string(message.subject);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JWTClaims {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJWTClaims();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.issuedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.issuer = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.subject = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JWTClaims {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      deviceId: isSet(object.deviceId)
        ? globalThis.String(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.String(object.device_id)
        : "",
      issuedAt: isSet(object.issuedAt)
        ? fromJsonTimestamp(object.issuedAt)
        : isSet(object.issued_at)
        ? fromJsonTimestamp(object.issued_at)
        : undefined,
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
      issuer: isSet(object.issuer) ? globalThis.String(object.issuer) : "",
      subject: isSet(object.subject) ? globalThis.String(object.subject) : "",
    };
  },

  toJSON(message: JWTClaims): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    if (message.issuedAt !== undefined) {
      obj.issuedAt = message.issuedAt.toISOString();
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    if (message.issuer !== "") {
      obj.issuer = message.issuer;
    }
    if (message.subject !== "") {
      obj.subject = message.subject;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JWTClaims>, I>>(base?: I): JWTClaims {
    return JWTClaims.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JWTClaims>, I>>(object: I): JWTClaims {
    const message = createBaseJWTClaims();
    message.userId = object.userId ?? "";
    message.deviceId = object.deviceId ?? "";
    message.issuedAt = object.issuedAt ?? undefined;
    message.expiresAt = object.expiresAt ?? undefined;
    message.issuer = object.issuer ?? "";
    message.subject = object.subject ?? "";
    return message;
  },
};

/**
 * AuthService provides authentication and token management for the IM system.
 * It validates JWT tokens and handles token refresh operations.
 */
export interface AuthService {
  /**
   * ValidateToken validates a JWT authentication token and extracts user claims.
   * Returns user_id and device_id if token is valid.
   */
  ValidateToken(request: ValidateTokenRequest): Promise<ValidateTokenResponse>;
  /**
   * RefreshToken generates a new access token using a valid refresh token.
   * Returns a new token pair with updated expiration times.
   */
  RefreshToken(request: RefreshTokenRequest): Promise<RefreshTokenResponse>;
}

export const AuthServiceServiceName = "auth.v1.AuthService";
export class AuthServiceClientImpl implements AuthService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || AuthServiceServiceName;
    this.rpc = rpc;
    this.ValidateToken = this.ValidateToken.bind(this);
    this.RefreshToken = this.RefreshToken.bind(this);
  }
  ValidateToken(request: ValidateTokenRequest): Promise<ValidateTokenResponse> {
    const data = ValidateTokenRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ValidateToken", data);
    return promise.then((data) => ValidateTokenResponse.decode(new BinaryReader(data)));
  }

  RefreshToken(request: RefreshTokenRequest): Promise<RefreshTokenResponse> {
    const data = RefreshTokenRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RefreshToken", data);
    return promise.then((data) => RefreshTokenResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
