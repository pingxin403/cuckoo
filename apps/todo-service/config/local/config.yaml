# Todo Service - Local Development Environment

server:
  grpc_port: 9091
  http_port: 8080
  host: "0.0.0.0"

observability:
  service_name: "todo-service"
  service_version: "1.0.0"
  environment: "local"
  log_level: "info"
  log_format: "json"
  enable_metrics: true
  metrics_port: 9090
