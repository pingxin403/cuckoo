# User Service - Local Development Environment

server:
  port: 9096
  host: "0.0.0.0"

database:
  host: "localhost"
  port: 3306
  user: "root"
  password: "root_password"
  database: "user_db"
  max_open_conns: 10
  max_idle_conns: 5

observability:
  service_name: "user-service"
  environment: "local"
  log_level: "debug"
  log_format: "text"
