# Auth Service - Testing Environment

server:
  port: 9095
  host: "0.0.0.0"

observability:
  service_name: "auth-service"
  environment: "testing"
  log_level: "info"
  log_format: "json"

jwt:
  secret: "testing-secret-from-env"
  expiration: 2h
  refresh_expiration: 72h
